"""Local command line tools.

The CLI exists for exactly one reason: importing an existing official seed
without that seed ever crossing HTTP. It runs in the user's own terminal, on
their own machine, against a file path they type themselves.
"""
